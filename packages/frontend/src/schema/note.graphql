mutation CreateNote($name: String!) {
  CreateNote(name: $name) {
    name
  }
}

mutation AddSource($from: String!, $to: String!) {
  AddNoteSources(from: { name: $from }, to: { link: $to }) {
    from {
      name
    }
    to {
      link
    }
  }
}
